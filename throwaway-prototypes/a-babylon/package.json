{
  "name": "@context-battler/throwaway-a-babylon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Throwaway Babylon.js telefrag renderer prototype.",
  "scripts": {
    "sync:harness": "node scripts/sync-shared-harness.mjs",
    "predev": "npm run sync:harness",
    "dev": "vite",
    "prebuild": "npm run sync:harness",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@babylonjs/core": "^9.8.0",
    "@babylonjs/loaders": "^9.8.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^5.4.11"
  }
}
